#include <stdio.h>
#include <stdlib.h>

int main()
{
    float distance;
    printf("entrer la distance en mile :");
    scanf("%f", &distance);
    printf("la distance en km : %f" , distance*1.609);

}
