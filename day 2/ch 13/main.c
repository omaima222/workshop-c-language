#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("entrer un nombre :");
    scanf("%d", &num);
    printf(" le nombre en octal est : %o\n", num);
    printf(" le nombre en hexadecimal est : %X\n", num);


        return 0;
}
